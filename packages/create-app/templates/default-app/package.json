{
  "name": "root-template",
  "private": true,
  "devDependencies": {
    "@types/fs-extra": "^9.0.8",
    "concurrently": "^6.0.0",
    "fs-extra": "^9.1.0",
    "lerna": "^4.0.0",
    "marked": ">=2.0.0",
    "ts-node": "^9.1.1",
    "typescript": "4.2.3"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "clean": "lerna clean --yes",
    "lint": "lerna run lint",
    "start": "concurrently \"yarn start-app\" \"yarn start-api\"",
    "start-with-mock-data": "yarn workspace @cloud-carbon-footprint/client start-with-mock-data",
    "start-app": "yarn workspace @cloud-carbon-footprint/client start",
    "start-api": "yarn workspace @cloud-carbon-footprint/api start",
    "start-cli": "yarn workspace @cloud-carbon-footprint/cli start",
    "test": "lerna run test -- --coverage",
    "guided-install": "cd packages/cli && yarn guided-install"
  }
}
