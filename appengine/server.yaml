service: api

runtime: nodejs12
env: flex

manual_scaling:
  instances: 1
resources:
  cpu: 1
  memory_gb: 0.5
  disk_size_gb: 10

env_variables:
  CCF_PORT: 8080
  CCF_CACHE_PATH: /tmp/estimates.cache.json
